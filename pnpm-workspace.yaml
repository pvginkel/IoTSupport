packages:
  - frontend
  # The cognitive-complexity sidecar used by tools/code_health:
  - tools/code_health/cognitive

# Packages allowed to run install scripts. pnpm blocks build scripts by
# default; list every dependency that legitimately needs one.
onlyBuiltDependencies:
  - esbuild
  - ssegateway
